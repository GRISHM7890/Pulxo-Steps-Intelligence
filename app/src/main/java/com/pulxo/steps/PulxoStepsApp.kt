package com.pulxo.steps

import android.app.Application

// In a real app we would annotate with @HiltAndroidApp
class PulxoStepsApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialization if needed
    }
}
